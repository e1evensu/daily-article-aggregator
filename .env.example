# 每日文章聚合器环境变量配置
# 复制此文件为 .env 并填入实际值

# OpenAI API配置（支持任何兼容OpenAI的API）
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.siliconflow.cn/v1
OPENAI_MODEL=deepseek-ai/DeepSeek-V3.2       # 模型名称，按你的API提供商填
OPENAI_MAX_TOKENS=
OPENAI_TEMPERATURE=0.7
OPENAI_TIMEOUT=60

# 飞书Webhook配置
FEISHU_WEBHOOK_URL=

# 飞书多维表格配置（用于数据可视化）
FEISHU_APP_ID=
FEISHU_APP_SECRET=
# 以下两项首次运行会自动创建，创建后填入可避免重复创建
FEISHU_BITABLE_APP_TOKEN=
FEISHU_BITABLE_TABLE_ID=

# 飞书事件订阅配置（用于知识库问答机器人）
# 在飞书开放平台 -> 应用 -> 事件订阅 中获取
FEISHU_VERIFICATION_TOKEN=
# 加密密钥（可选，如果启用了消息加密）
FEISHU_ENCRYPT_KEY=

# 代理配置（可选，用于访问被墙的网站）
HTTP_PROXY=
HTTPS_PROXY=

# Playwright配置（用于反爬网站）
# 普通请求失败时自动 fallback 到 Playwright
# 设置为 true 则所有请求都用 Playwright（更慢但更稳）
PLAYWRIGHT_ALWAYS=false
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_TIMEOUT=30000
